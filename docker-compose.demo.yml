services:
  api:
    environment:
      MEDCLAIM_ENV: demo
      LLM_PROVIDER: fake
      LLM_MODEL: fake-verifier-v1
      PERSISTENCE_ENABLED: "false"
      PERSIST_CLAIM_TEXT: "false"
      PERSIST_EXPLANATION: "false"
  streamlit:
    environment:
      MEDCLAIM_API_URL: http://api:8000
